package org.starmod.net.handler;

import org.starmod.net.Client;
import org.starmod.net.Handler;
import org.starmod.net.command.MessageTo;

public class MessageToHandler implements Handler<MessageTo> {

	@Override
	public void handle(Client client, MessageTo cmd) {

	}

}
